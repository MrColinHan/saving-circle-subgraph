{
  "name": "saving-circle-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ mrcolinhan/saving-circle-subgraph",
    "create-local": "graph create --node http://localhost:8020/ mrcolinhan/saving-circle-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ mrcolinhan/saving-circle-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mrcolinhan/saving-circle-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
