type ExampleEntity @entity {
  id: ID!
  count: BigInt!
  user: Bytes! # address
  circle: Bytes! # bytes32
}

type ContributionMade @entity {
  user: Bytes! # address
}

type RequestGranted @RequestGranted {
  user: Bytes! # address
}

type RequestMade @RequestMade {
  user: Bytes! # address
}
